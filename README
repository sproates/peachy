Peachy is an interpreted scripting language.

Software design goals:

- Don't use any external dependencies
- Have complete documentation
- Have a complete test suite

Language design goals:

- Support XML as a language construct (a la Scala)
- Support JSON as a language construct (a la JavaScript)
- Everything is an object (a la Ruby)
